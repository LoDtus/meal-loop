import ProfileLayout from "@/fragments/profile/ProfileLayout";

// Chuyển luôn người dùng tới username của người đó → t lười tạo các router con cho id/... lắm
export default function ProfilePageById() {
    return (
        <ProfileLayout/>
    );
};