
/**
 * Đang dùng: npm i next-nprogress-bar
 * Đề xuất:
 * - react-top-loading-bar: có thể phản ánh đúng tiến độ loading thật
 * - nprogress: cổ, phổ biến nhất và nhẹ
 * - topbar: nhẹ nhất
 */
export default function LoadingBar() {
    return (
        <div>

        </div>
    );
};